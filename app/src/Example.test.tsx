import React from "react";

describe("simple tests", () => {
  test("two plus two is four", () => {
    expect(2 + 2).toBe(4);
  });

  test("two minus one is one", () => {
    expect(2 - 1).toBe(1);
  });
});
